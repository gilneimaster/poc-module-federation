import { MfeBasePathPipe } from './mfe-base-path.pipe';

describe('MfeBasePathPipe', () => {
  it('create an instance', () => {
    const pipe = new MfeBasePathPipe();
    expect(pipe).toBeTruthy();
  });
});
